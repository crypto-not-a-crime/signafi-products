{
  "name": "signafi-products",
  "private": true,
  "workspaces": [
    "web",
    "worker"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "dev:web": "npm run dev -w web",
    "dev:worker": "npm run dev -w worker",
    "build": "npm run build -w web && npm run build -w worker",
    "test": "npm run test -w worker"
  }
}
